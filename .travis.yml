
sudo: required
language: java

jdk:
  - openjdk8

script:
  - mvn package -DskipTests

before_deploy:
    - mkdir MiningBuild
    - find . -name '*.jar' -exec cp {} ./MiningBuild \;
    - cd /home/travis/build/galilasmb/jsoup/MiningBuild
    - tar -zcvf result.tar.gz *
deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file: result.tar.gz
  name: fetchjar-821b021a1e5fc75719c71ae5ea84157d770c1a9d
  file_glob: true
  overwrite: false
  skip_cleanup: true
  on:
    all_branches: true 
            